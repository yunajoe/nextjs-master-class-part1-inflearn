export default function SearchForm({
  initialQuery,
}: {
  initialQuery?: string;
}) {
  // TODO 1: 자바스크립트(useState) 없이 HTML <form>과 action, name="q" 속성을 활용해 검색창을 구현하세요.
  // TODO 2: initialQuery를 받아 검색창의 defaultValue로 설정하여 상태를 유지하세요.
  return (
    <div className="p-4 border border-red-300 bg-red-50 text-red-600 rounded-md">
      여기에 SearchForm 컴포넌트를 구현해주세요.
    </div>
  );
}
