# 💡 HTML Form 제출과 Input 값 처리 메커니즘 정리

사용자가 `<input>`에 명시적인 `value` 속성을 적지 않았음에도 어떻게 브라우저가 자동으로 `/posts?q=입력한검색어`로 이동시킬 수 있는지, 그 핵심 메커니즘과 동작 원리를 정리한 내용입니다.

---

## 1. 핵심 요약

HTML `<form>` 태그와 `<input>` 태그가 가진 **브라우저 네이티브 폼 제출(Form Submission) 동작** 덕분입니다. 개발자가 직접 JavaScript 코드를 작성하거나 `router.push()`를 호출하지 않아도, 브라우저가 알아서 폼 내부의 입력 데이터를 수집해 URL에 반영합니다.

---

## 2. 상세 메커니즘 4단계

### ① `name="q"` 속성 부여

- `<input name="q" type="text" />`와 같이 `name` 속성을 지정하면, 브라우저는 이 인풋 요소를 폼 데이터의 **'키(Key)'**로 인식하고 등록합니다.

### ② 사용자의 타이핑과 DOM 상태 (Value Property)

- 사용자가 키보드로 인풋창에 글자를 입력하면, 리액트 상태(`useState`)나 `value` 속성이 코드에 없더라도 **브라우저 자체 DOM 내부(Value Property)에 입력한 문자열이 실시간으로 보관**됩니다.
- 즉, HTML Attribute(`value="..."`)와 브라우저 메모리상의 DOM Property(`input.value`)는 다릅니다. 브라우저는 폼 제출 시 **실시간 DOM Property**를 읽어옵니다.

### ③ `type="submit"`과 폼 제출

- 사용자가 '검색' 버튼(`type="submit"`)을 누르거나 인풋창에서 엔터를 치면, 브라우저는 `<form>` 태그의 `action` 주소를 확인하고 서버로 이동할 준비를 합니다.
- 이때 브라우저는 폼 내부를 탐색하며 **`name` 속성이 있는 요소들을 찾아 그 안에 들어있는 값(`value`)을 수집**합니다.
  - 예: `name="q"` 이고 입력된 값이 `"리액트"`라면 $
ightarrow$ `q=리액트` 조합 생성

### ④ Query String 자동 생성 및 페이지 이동

- GET 방식(기본값)으로 폼이 제출될 때, 브라우저는 수집한 데이터를 `action` 주소 뒤에 `?키=값` 형태로 직접 붙여서 이동시킵니다.
- 결과적으로 브라우저 주소창이 자동으로 `/posts?q=리액트`로 갱신됩니다.

---

## 3. 핵심 개념 비교: `value` vs `defaultValue`

이 메커니즘을 이해하면 React/Next.js 환경에서 어떤 속성을 써야 하는지 명확해집니다.

- **`value` 사용 시 (제어 컴포넌트)**
  - React가 인풋의 값(DOM Property)을 완전히 통제합니다.
  - `onChange` 이벤트 핸들러가 없다면 사용자가 키보드로 입력해도 **글자가 입력되지 않거나 튕기는 현상**이 발생합니다.
- **`defaultValue` 사용 시 (비제어 컴포넌트)**
  - 페이지가 처음 로드되거나 서버에서 리렌더링될 때만 초기 텍스트를 잡아줍니다.
  - 이후 사용자의 타이핑은 브라우저의 네이티브 DOM 상태에 온전히 맡기므로, 순수 HTML 폼 제출 방식과 완벽하게 호환됩니다.

```

`defaultValue`는 컴포넌트가 **처음 마운트(렌더링)될 때의 초기값만 설정**해주고, 그 이후에 **URL이나 외부 props가 바뀌어도 인풋창의 값을 강제로 덮어쓰지 않기 때문**입니다.

이러한 동작 방식은 다음과 같은 메커니즘으로 이루어집니다.

1. **`defaultValue`의 역할**: 리액트에서 `defaultValue`는 비제어 컴포넌트(Uncontrolled Component)의 초기값을 설정합니다. 즉, 화면에 처음 렌더링될 때 딱 한 번만 `<input>`의 DOM Property(`value`)를 지정하고, 그 이후의 입력값 관리는 브라우저(DOM)에 완전히 위임합니다.
2. **URL이 바뀌어도 인풋이 그대로 유지되는 이유**: 사용자가 검색어를 입력하고 엔터를 치거나 버튼을 눌러 주소가 `/posts?q=리액트`로 바뀌면, 페이지가 새로고침되거나 리렌더링되면서 `initialQuery`에 새로운 값이 들어옵니다. 하지만 비제어 컴포넌트인 인풋은 **이미 브라우저 DOM 내부에 사용자가 입력하거나 서버가 준 기존의 상태를 들고 있기 때문에**, `defaultValue`가 바뀐다고 해서 인풋창의 글자가 강제로 초기화되거나 리셋되지 않고 유지됩니다.

만약 외부에서 `initialQuery` 값이 바뀔 때마다 인풋창의 값도 강제로 그 값에 맞춰 리셋(변경)되게 만들고 싶다면, `defaultValue` 대신 제어 컴포넌트 방식인 `value={initialQuery}`를 사용해야 합니다. (다만 이 경우 앞서 다뤘던 것처럼 인풋에 타이핑을 하기 위해 `onChange` 이벤트 핸들러가 함께 구현되어야 합니다.)

```

## 4. **`button type="submit"`이 아니라면?**

- **`type="button"`**: 브라우저는 이 버튼을 아무런 폼 제출 기능이 없는 **일반 버튼**으로 취급합니다. 버튼을 눌러도 `form`의 `action`으로 이동하지 않고 데이터도 제출되지 않습니다. (보통 onClick 이벤트로 자바스크립트 함수를 실행할 때 사용합니다.)
- **`type="reset"`**: 버튼을 누르면 폼 내부의 모든 입력값이 사용자가 처음 입력하기 전(초기 상태)으로 리셋(초기화)됩니다.
- **`type`을 아예 생략한 경우**: HTML 표준상 `<button>`의 기본값은 `submit`입니다. 따라서 `type`을 적지 않아도 `submit`으로 동작하여 폼이 제출됩니다. (단, `<form>` 내부가 아닌 일반 위치에서는 브라우저마다 미묘하게 다를 수 있으므로 명시하는 것이 좋습니다.)

---

## 5. 폼 제출 방식: GET vs POST

GET 방식 외에 폼 제출에서 가장 널리 쓰이는 대표적인 방식은 **`POST` 방식**입니다.

### 1. POST 방식이란?

- **동작 방식**: 입력한 데이터를 URL 주소창에 `?q=리액트` 형태로 노출시키지 않고, HTTP Request Body(본문)의 숨겨진 공간에 담아서 서버로 전송합니다.
- **주요 용도**:
- 비밀번호, 개인정보 등 **보안이 중요한 데이터**를 전송할 때
- 게시글 작성, 회원가입, 결제 등 **서버의 데이터에 변화(생성/수정/삭제)를 주는 작업**을 할 때

### 2. 구별하는 방법 (`method` 속성)

HTML `<form>` 태그의 **`method` 속성**을 통해 구별하고 지정할 수 있습니다.

```tsx
{
  /* GET 방식 (기본값: 생략하면 GET으로 동작) */
}
<form action="/posts" method="get">
  <input name="q" />
  <button type="submit">GET 검색</button>
</form>;

{
  /* POST 방식 */
}
<form action="/posts" method="post">
  <input name="password" type="password" />
  <button type="submit">POST 전송</button>
</form>;
```

- **`method="get"` (또는 생략)**: 데이터가 주소창(Query String)에 노출되며, 주로 데이터를 조회(Search/Read)할 때 사용합니다. 새로고침을 해도 안전합니다.
- **`method="post"`**: 데이터가 본문(Body)에 숨겨져서 전달되며, 주로 **데이터를 생성하거나 변경**할 때 사용합니다.

## 6. Search Form 정렬(sort) 상태 유지

- Next.js 환경에서 서버 컴포넌트와 HTML 네이티브 <form>(method="GET")을 조합하여 검색 기능을 구현할 때, 검색어를 입력하면 기존 정렬(sort) 상태가 초기화되는 이슈의 원인과 해결 방법

### 원인 분석

- HTML <form method="GET" action={url}>은 폼이 제출될 때 오직 <form> 내부의 input 태그들만 수집하여 쿼리 스트링을 만듭니다.

페이지 컴포넌트(page.tsx)에서 url이나 다른 링크(sortHref)를 통해 sort 값을 관리하고 있더라도, SearchForm 내부의 form 자체는 외부의 sort 상태를 인지하지 못합니다.

결과적으로 검색을 수행하면 브라우저는 오직 <input name="q">의 값만 들고 이동하므로, URL에서 sort 파라미터가 누락됩니다.

### 해결 방법

- 외부(props)로 전달받은 sort 값이 존재할 경우, 폼 내부에 숨겨진 인풋(<input type="hidden">)을 배치하여 폼 제출 시 함께 전송되도록 처리합니다.

```javascript


export default function SearchForm({
initialQuery,
url,
sort,
}: {
initialQuery?: string;
url?: string;
sort?: string;
}) {
return (
<form action={url} method="GET" className="w-full flex items-center gap-2">
{/_ 👈 핵심: sort 값이 존재할 경우 hidden input으로 함께 전송 _/}
{sort && <input type="hidden" name="sort" value={sort} />}

      <input
        name="q"
        type="text"
        placeholder="🔍 검색어 입력..."
        className="border-2 border-blue-400 rounded-sm w-full"
        defaultValue={initialQuery}
      />
      <button
        type="submit"
        className="w-28 border-2 border-gray-800 bg-black text-white"
      >
        검색
      </button>
    </form>

);
}

```

## 7. 검색어가 빈 값일때의 url 처리

### 해결 방법

- HTML <form>은 기본적으로 입력값이 비어 있더라도 name="q"가 존재하면 빈 문자열(?q=)을 쿼리 스트링에 포함하여 제출합니다. 이를 방지하려면 JavaScript의 onSubmit 이벤트를 사용하여 빈값일 때 해당 input의 name 속성을 임시로 제거하거나, 빈값일 때 q= 파라미터가 아예 생기지 않도록 처리해야 합니다. 가장 깔끔한 방법은 제출 직전에 값이 비어 있는 input의 name 속성을 지우는 것

```javascript
export default function SearchForm({
  initialQuery,
  url,
  sort,
}: {
  initialQuery?: string;
  url?: string;
  sort?: string;
}) {
  return (
    <form
      action={url}
      method="GET"
      className="w-full flex items-center gap-2"
      onSubmit={(e) => {
        const form = e.currentTarget;
        const input = form.elements.namedItem('q') as HTMLInputElement;

        // 검색어가 비어있거나 공백만 있는 경우 name 속성을 제거하여 url에 q=가 안 생기게 함
        if (input && !input.value.trim()) {
          input.name = '';
        }
      }}
    >
      {sort && <input type="hidden" name="sort" value={sort} />}
      <input
        name="q"
        type="text"
        placeholder="🔍 검색어 입력..."
        className="border-2 border-blue-400 rounded-sm w-full"
        defaultValue={initialQuery}
      />
      <button
        type="submit"
        className="w-28 border-2 border-gray-800 bg-black text-white"
      >
        검색
      </button>
    </form>
  );
}

```
